model User {
  id       Int     @id @default(autoincrement())
  fullname String
  email    String
  username String
  password String

  @@map("ngdrstab_mst_user")  // Map this model to the `users` table in your database
}